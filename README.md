A small floating app for local AI assistant on macOS.
Inspired by OpenAI's design for ChatGPT app.

It simply loads a web view, such as Open WebUI.
For now, configure a shortcut with skhd to toggle it.
